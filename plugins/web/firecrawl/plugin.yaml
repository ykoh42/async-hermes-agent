name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Requires FIRECRAWL_API_KEY or FIRECRAWL_API_URL for a self-hosted instance."
author: NousResearch
kind: backend
provides_web_providers:
  - firecrawl
